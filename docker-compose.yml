services:
  shiro-automation-ui:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3222:3222"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped
